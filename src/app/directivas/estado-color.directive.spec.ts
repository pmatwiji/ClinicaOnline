import { EstadoColorDirective } from './estado-color.directive';

describe('EstadoColorDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoColorDirective();
    expect(directive).toBeTruthy();
  });
});
